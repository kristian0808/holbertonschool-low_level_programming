#include "main.h"
/**
 * add - Entry point
 * @a: int to be added to b
 * @b: int to be added to a
 * Return the sum of two int numbers;
 * Return: 0
 */

int add(int a, int b)
{
	int add;

	add = a + b;
	return (add);
}
